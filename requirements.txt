streamlit>=1.64,<2
SQLAlchemy>=2.1,<3
pandas>=3.0,<4
plotly>=7.1,<8
python-dotenv>=1.2,<2
# Driver Postgres (default di SQLAlchemy 2.1 per gli URL postgresql://): serve solo con Supabase
psycopg[binary]>=3.3,<4
# Agente di scouting: API Claude, chiamate HTTP alle fonti e a Telegram, lista aziende in YAML
anthropic>=1.8,<2
requests>=2.32,<3
PyYAML>=6.0,<7
